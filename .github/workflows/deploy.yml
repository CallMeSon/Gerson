name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy and rebuild Docker
        run: |
          cd /home/econ/Gerson
          git pull origin main
          docker compose up -d --build
